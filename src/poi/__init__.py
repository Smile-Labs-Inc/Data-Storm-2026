"""Stub package -- the actual POI implementation lives in
`competition/poi_pipeline/` (separate folder by user request).

Use:
    poi_features = pd.read_parquet(
        "../poi_pipeline/output/poi_features.parquet"
    )
"""
